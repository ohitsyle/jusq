# Copy to client/.env. Relative URLs = same origin as the server that serves
# the app, so the build works on any IP or domain without rebuilding.
VITE_API_URL=/api
VITE_BACKEND_URL=
VITE_GOOGLE_MAPS_API_KEY=
